<div class="container comment-container" id="comment-<%=comment.id%>">
    <i class="author text-primary">
      <span><%= comment.user.first_name %></span>
      <small><%= comment.created_at.strftime("%a %b %e at %l:%M%p") %>:</small>
    </i>
    <p><%= comment.content%></p>
  </div>
